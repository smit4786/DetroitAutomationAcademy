# =============================================================================
# DETROIT AUTOMATION ACADEMY - ENVIRONMENT VARIABLES
# =============================================================================
# ⚠️  SECURITY WARNING: DO NOT COMMIT THE ACTUAL .env FILE TO GIT
# This is a template file. Copy to .env and fill in your actual values.
#
# Setup Instructions:
#   1. Run secure setup: ./setup_google_calendar_secure.sh
#   2. Or manually: cp .env.example .env
#   3. Fill in actual values in .env (see SETUP_GOOGLE_SERVICES_SECURE.md)
#   4. NEVER commit .env to version control
#   5. The .env file is already listed in .gitignore for protection
# =============================================================================

# -----------------------------------------------------------------------------
# GOOGLE CALENDAR API CREDENTIALS
# -----------------------------------------------------------------------------
# Google account email used for calendar operations
# CRITICAL: This should NEVER be hardcoded in scripts or public documentation
# INTERNAL ONLY: Contact team lead for actual value
GOOGLE_ACCOUNT_EMAIL=[REDACTED - Your Google account email]

# Google Calendar ID (usually same as account email or custom calendar ID)
# INTERNAL ONLY: Contact team lead for actual value
GOOGLE_CALENDAR_ID=[REDACTED - Your calendar ID or email]

# Path to Google API credentials JSON file
# Download from: https://console.cloud.google.com/apis/credentials
# SECURITY: This file contains sensitive OAuth credentials
GOOGLE_API_CREDENTIALS_FILE=credentials.json

# Path to stored OAuth token (generated automatically on first run)
# SECURITY: This file is auto-generated and contains authentication tokens
GOOGLE_TOKEN_FILE=token.pickle

# -----------------------------------------------------------------------------
# GOOGLE DRIVE API (Future Use)
# -----------------------------------------------------------------------------
# GOOGLE_DRIVE_FOLDER_ID=[REDACTED - Your Drive folder ID]

# -----------------------------------------------------------------------------
# GITHUB INTEGRATION
# -----------------------------------------------------------------------------
# GitHub Personal Access Token for API access
# Generate at: https://github.com/settings/tokens
# Required scopes: repo, workflow (for Actions)
# SECURITY: Treat this like a password - NEVER commit to git
GITHUB_TOKEN=[REDACTED - Your GitHub token]

# GitHub Repository Details
# INTERNAL ONLY: Contact team lead for actual values
GITHUB_REPO_OWNER=[REDACTED - Repository owner username]
GITHUB_REPO_NAME=DetroitAutomationAcademy

# -----------------------------------------------------------------------------
# OPERATIONAL SETTINGS
# -----------------------------------------------------------------------------
# Application environment (development, staging, production)
APP_ENV=development

# Timezone for calendar operations
TZ=America/Detroit

# -----------------------------------------------------------------------------
# SECURITY REMINDERS
# -----------------------------------------------------------------------------
# ✅ ALWAYS use environment variables for sensitive data
# ✅ NEVER commit credentials.json or token.pickle to git
# ✅ NEVER hardcode emails, tokens, or API keys in scripts
# ✅ REVIEW all commits for exposed credentials before pushing
# ✅ USE placeholder [REDACTED] in all public documentation
# ✅ ROTATE credentials every 90 days
# ✅ SHARE credentials only via secure password manager
# 
# For full security procedures, see:
#   - AGENT_SECURITY_PROCEDURES.md (for AI agents)
#   - SECURITY_PROCEDURES.md (for developers)
#   - SETUP_GOOGLE_SERVICES_SECURE.md (INTERNAL ONLY - setup guide)
# =============================================================================
